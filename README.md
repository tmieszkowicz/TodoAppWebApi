# Todo API

Web API for managing to-do tasks. Authentication via JWT, role-based authorization, health checks, and structured logging. The API provides CRUD operations for tasks, ensuring user-specific data access.
